@using Microsoft.AspNetCore.Identity
@inject SignInManager<CetUser> SignInManager
@inject UserManager<CetUser> UserManager
@* @inject KulsayfasiViewComponent KulsayfasiComponent *@
@* 
@await KulsayfasiComponent.InvokeAsync() *@

@{
    var userId = UserManager.GetUserId(User);
    
}

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    @* <li class="nav-item">
        <a  class="nav-link " asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li> *@
        
        <li class="nav-item">
            <a class="nav-link " asp-area="" asp-controller="Kulsayfasis" asp-action="Details" asp-route-id="1">Hello @User.Identity?.Name!</a>
        </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link ">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link " asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link " asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>
